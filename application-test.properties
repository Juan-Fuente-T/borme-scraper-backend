# --- PERFIL DE TEST CONTRA DOCKER ---

# Coordenadas de la Base de Datos PostgreSQL corriendo en Docker
spring.datasource.url=jdbc:postgresql://localhost:5432/borme_db
spring.datasource.username=user
spring.datasource.password=password
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Flyway desactivado en tests
spring.flyway.enabled=falsee